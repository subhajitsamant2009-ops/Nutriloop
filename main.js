/* ─── MAIN ───────────────────────────────────────────────────── */
// Initialise all modules on DOMContentLoaded
document.addEventListener('DOMContentLoaded', () => {
  renderFoodGrid();
  renderLog();
  renderWater();
  renderExercise();
});
